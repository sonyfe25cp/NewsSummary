package tmp;

import java.util.List;

import org.ansj.domain.Term;
import org.ansj.splitWord.analysis.BaseAnalysis;
import org.ansj.splitWord.analysis.NlpAnalysis;
import org.ansj.splitWord.analysis.ToAnalysis;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;


public class TestAnsj {

    static Logger logger = LoggerFactory.getLogger(TestAnsj.class);
    public static void main(String[] args) {
              String str = "The sheriff's initial estimate of as many as 25 dead in the Columbine High massacre" +
              		" was off the mark apparently because the six SWAT teams that swept the building counted some victims more than once";
//              JobTitle jt = new JobTitle(str);

//              List<Term> nlpparse = NlpAnalysis.parse(str);
//              System.out.println(nlpparse);
//
//              List<Term> toparse = ToAnalysis.parse(str);
//              System.out.println(toparse);
//              for(Term term : toparse){
//                  logger.info("name: {}, nature: {}",term.getName(), term.getNatrue().natureStr);
//              }
//        
//              List<Term> parse = BaseAnalysis.parse(str);
//              System.out.println(parse);
              
              
              
    }

}
